"use client";
import { useEffect,useState } from "react"; import { useRouter } from "next/navigation"; import { Bell,LogOut,Menu,Search } from "lucide-react"; import { api,AuthUser,logout } from "@/lib/api";
export function Topbar(){const router=useRouter();const [user,setUser]=useState<AuthUser>({name:"Renan Braga",plan:"Starter"});useEffect(()=>{void api.me().then(setUser).catch(()=>{const raw=localStorage.getItem("hunter_user");if(raw)try{setUser(JSON.parse(raw))}catch{}})},[]);const initials=(user.name??"Hunter User").split(" ").slice(0,2).map(x=>x[0]).join("").toUpperCase();return <div className="animate-premium-in mb-7 flex items-center justify-between gap-4"><button className="flex h-11 w-11 items-center justify-center rounded-2xl bg-white/80 text-[#53617f] shadow-sm lg:hidden"><Menu className="h-5 w-5"/></button><div className="hidden h-11 max-w-[420px] flex-1 items-center gap-3 rounded-2xl border border-white/80 bg-white/70 px-4 shadow-sm backdrop-blur-xl md:flex"><Search className="h-4 w-4 text-[#95a0b6]"/><input placeholder="Buscar viagens, alertas ou insights..." className="w-full bg-transparent text-sm outline-none placeholder:text-[#9aa5ba]"/></div><div className="ml-auto flex items-center gap-3"><button onClick={()=>router.push("/alerts")} className="relative flex h-11 w-11 items-center justify-center rounded-2xl border border-white/80 bg-white/75 text-[#596784]"><Bell className="h-4 w-4"/></button><div className="flex items-center gap-3 rounded-2xl border border-white/80 bg-white/75 px-3 py-2"><span className="flex h-8 w-8 items-center justify-center rounded-xl bg-gradient-to-br from-[#2d6bff] to-[#7956ff] text-xs font-bold text-white">{initials}</span><span className="hidden text-left sm:block"><span className="block text-xs font-bold text-[#172241]">{user.name??user.email??"Usuário"}</span><span className="block text-[10px] text-[#8390aa]">{user.plan??"Starter"}</span></span><button title="Sair" onClick={()=>{logout();router.push("/login")}} className="ml-1 text-[#8390aa]"><LogOut className="h-4 w-4"/></button></div></div></div>}
