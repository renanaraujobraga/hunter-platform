"use client";
import { FormEvent,useState } from "react"; import { useRouter } from "next/navigation"; import { Plane } from "lucide-react"; import { api,saveAuth } from "@/lib/api";
export default function LoginPage(){const r=useRouter();const [register,setRegister]=useState(false);const [name,setName]=useState("");const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [error,setError]=useState("");const [busy,setBusy]=useState(false);
 async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError("");try{const result=register?await api.register(name,email,password):await api.login(email,password);saveAuth(result);r.push("/");r.refresh()}catch(e){setError(e instanceof Error?e.message:"Falha na autenticação")}finally{setBusy(false)}}
 return <main className="flex min-h-screen items-center justify-center bg-[#f5f7ff] p-5"><div className="w-full max-w-md rounded-[32px] border border-white bg-white/90 p-8 shadow-[0_30px_90px_rgba(53,68,120,.14)]"><div className="flex items-center gap-3"><span className="flex h-12 w-12 items-center justify-center rounded-2xl bg-gradient-to-br from-[#1557ff] to-[#7b4dff] text-white"><Plane className="h-6 w-6 -rotate-12"/></span><div><h1 className="text-2xl font-bold text-[#0a1434]">Hunter AI</h1><p className="text-xs text-[#7080a5]">Inteligência de viagens</p></div></div><h2 className="mt-8 text-2xl font-bold">{register?"Criar conta":"Entrar"}</h2><form onSubmit={submit} className="mt-5 space-y-4">{register&&<input required value={name} onChange={e=>setName(e.target.value)} placeholder="Nome" className="h-12 w-full rounded-2xl border px-4"/>}<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="E-mail" className="h-12 w-full rounded-2xl border px-4"/><input required type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Senha" className="h-12 w-full rounded-2xl border px-4"/>{error&&<p className="text-sm text-red-600">{error}</p>}<button disabled={busy} className="h-12 w-full rounded-2xl bg-[#101a3a] font-bold text-white">{busy?"Aguarde...":register?"Criar conta":"Entrar"}</button></form><button onClick={()=>setRegister(!register)} className="mt-5 w-full text-sm font-bold text-[#1557ff]">{register?"Já tenho conta":"Criar uma conta"}</button></div></main>}
